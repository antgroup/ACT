# 贡献指南

感谢你对 ACT 协议的关注！本文档说明如何参与协议的演进和改进。

## 贡献流程

1. **提出问题**：在 GitHub Issues 中提交问题或建议，描述现状和期望的改进方向。
2. **提交提案**：对于规范变更，先提交 Proposal，说明变更动机、影响范围和具体方案。
3. **起草草案**：维护者评审通过后，提交 Draft PR，包含完整的规范修改和对应的 Schema/示例更新。
4. **社区评审**：Draft PR 开放至少 **2 周**的社区评审期，收集反馈并迭代修改。
5. **合并发布**：维护者团队投票通过（2/3 多数）后合并，并更新版本号。

## 规范变更类型

| 变更类型 | 说明 | 评审期 |
|---------|------|--------|
| 勘误 | 修正文档中的错误，不影响语义 | 1 周 |
| 澄清 | 明确模糊表述，不改变行为 | 1 周 |
| 新增 | 添加新字段、新组件、新事件类型 | 2 周 |
| 破坏性变更 | 修改或删除已有字段的语义 | 4 周，需附带迁移指南 |

## 提交规范

提交消息使用以下前缀：

- `feat(specs):` — 规范文档新增或修改
- `feat(schema):` — JSON Schema 新增或修改
- `feat(impl):` — 参考实现新增或修改
- `fix(specs):` — 规范文档勘误
- `fix(schema):` — JSON Schema 修复
- `fix(impl):` — 参考实现修复
- `docs:` — 文档改进
- `chore:` — 构建、CI 等杂项

## PR 要求

- 规范文档变更须同步更新对应的 JSON Schema
- JSON Schema 变更须同步更新对应的示例报文
- 新增字段须提供至少一个示例
- 破坏性变更须附带迁移指南

## CLA

所有贡献者须签署 Contributor License Agreement（CLA），确保贡献内容的知识产权清晰。首次提交 PR 时，CLA 机器人将自动引导签署流程。